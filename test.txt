json.dumps(​obj)
json.dumps(​obj, indent=4)
json.dumps(​obj, indent=4, sort_keys=True if pretty else False)
json.dumps(​obj, cls=json.JSONEncoder if self.compact else DateJSONEncoder)
json.dumps(​obj, default)
